services:
  - type: web
    name: streamslicer
    env: node
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
